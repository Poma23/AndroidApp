<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.androidexam">
    <!-- using internet -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">


        <activity android:name=".UniList"/>


        <activity android:name=".Maritime" />
        <activity android:name=".Artistic" />
        <activity android:name=".University_collage" />

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".Home_fragment" />
        <activity android:name=".Accomodation_fragment" />
        <activity android:name=".Search_fragment" />
        <activity android:name=".Scholar" />
        <activity android:name=".Login_fragment" />
        <activity android:name=".Email_fragment" />
        <activity android:name=".Apply" />
        <activity android:name=".Register" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>